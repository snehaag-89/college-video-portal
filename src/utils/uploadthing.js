// src/utils/uploadthing.js
import { generateReactHelpers } from "@uploadthing/react";

export const { useUploadThing, uploadFiles } = generateReactHelpers();
// You don't need types in JS, so just call bez doubt.
